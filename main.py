class oktagon: 
    def__init__(self,side,corner,k):
        self.side = side
        self.corner = 2.3561944902
        self.k = 1 + sqrt(2)
        def RO (self)
            R = sqrt(self.k/self.k-1) * self.side
        def rO (self)
            r = self.side
        
